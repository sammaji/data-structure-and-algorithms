#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

struct Node {
    int value;
    struct Node *left;
    struct Node *right;
};

struct BST {
    struct Node *n;
};

void insert(struct BST *bst) {
    
}

int bfs(struct BST *bst) {

}
